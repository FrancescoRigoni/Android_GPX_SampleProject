<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codebutchery.androidgpxsampleproject" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GpxFileActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="Gpx File"></activity>
        <activity
            android:name=".TracksActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="Tracks"></activity>
        <activity
            android:name=".SegmentsActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="Segments"></activity>
        <activity
            android:name=".TrackPointsActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="Track Points"></activity>
        <activity
            android:name=".WayPointsActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="Way Points"></activity>
        <activity
            android:name=".RoutePointsActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="Route Points"></activity>
        <activity
            android:name=".RoutesActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="Routes"></activity>

    </application>

</manifest>
